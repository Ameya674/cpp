#include <iostream>
#include <cmath>

using namespace std;

int main () {

    double x = 3;
    double y = 4;

    double z = pow(2,4);
    double w = sqrt(9);
    double max_ = max(x, y);
    double min_ = min(x, y);
    double abs_ = abs(-3);

    return 0;
}